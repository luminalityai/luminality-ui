name: Dependabot auto-merge

# Inlined from luminalityai/.github's reusable dependabot-auto-merge.yml
# (@7450c1ec, main @ 2026-09-24). .github is private, and GitHub forbids this
# PUBLIC repo from calling a reusable workflow stored in a private repo (the
# call resolves as "workflow was not found") -- the same reason pin-gate.yml is
# inlined. Keep in sync with the org copy when it changes.
#
# Why the org version and not the previous inline one: it merges with the
# luminality-release-bot App token (falling back to GITHUB_TOKEN loudly). A
# merge made with GITHUB_TOKEN fires no `push` event, so the old lander landed
# PRs on main without running pages.yml.

on: pull_request_target

permissions: {}

concurrency:
  group: dependabot-auto-merge-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  auto-merge:
    # Act on Dependabot's own PRs and on autonomous release-bot PRs. The bot
    # path gates on BOTH the head-ref prefix AND the actor being the release-bot
    # App (luminality-release-bot[bot]) — the branch prefix alone is spoofable:
    # anyone who can push a branch named ci-fix/* (etc.) would otherwise get
    # native auto-merge enabled with no human review. Requiring the App actor
    # closes that branch-name-spoofing hole, since only these workflows can
    # open PRs as the release-bot.
    if: >-
      github.actor == 'dependabot[bot]' ||
      ( github.actor == 'luminality-release-bot[bot]' && (
        startsWith(github.event.pull_request.head.ref, 'issue-worker/') ||
        startsWith(github.event.pull_request.head.ref, 'ci-fix/') ||
        startsWith(github.event.pull_request.head.ref, 'sentry-autofix/') ||
        startsWith(github.event.pull_request.head.ref, 'legal-sync/') ) )
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Mint a release-bot App token for the merge
        id: app_token
        # `continue-on-error` is load-bearing: this action FAILS the step when the
        # client-id / private key is missing or the App is not installed on the
        # calling repo. On failure `steps.app_token.outputs.token` is empty and the
        # enable below falls back (loudly) to GITHUB_TOKEN instead of the whole
        # lander going down for that PR.
        continue-on-error: true
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: ${{ vars.RELEASE_BOT_CLIENT_ID }}
          private-key: ${{ secrets.RELEASE_BOT_PRIVATE_KEY }}
          # Scoped down to exactly what the enable needs.
          permission-contents: write
          permission-pull-requests: write

      - name: Resolve the merge token (App → GITHUB_TOKEN)
        id: token
        # Runs on every path so the decision is always recorded. Only a
        # "true"/"false" comparison is interpolated — never a token.
        env:
          HAS_APP: ${{ steps.app_token.outputs.token != '' }}
        run: |
          set -euo pipefail
          if [ "$HAS_APP" = "true" ]; then
            echo "mode=release-bot App" >> "$GITHUB_OUTPUT"
            {
              echo "### Auto-lander merge token"
              echo ""
              echo "✅ Merge token: the \`luminality-release-bot\` **App installation token**. If this run enables auto-merge (see the enable step), the merge is attributed to the App, so downstream \`push\` / \`workflow_run\` workflows (deploy, sentry-release) will fire."
            } >> "$GITHUB_STEP_SUMMARY"
          else
            echo "mode=GITHUB_TOKEN (degraded)" >> "$GITHUB_OUTPUT"
            echo "::warning::No luminality-release-bot App token — auto-merge falls back to GITHUB_TOKEN, so the merge fires NO push event (deploy/sentry-release will not run). See the job summary."
            {
              echo "### Auto-lander merge token"
              echo ""
              echo "⚠️ **Degraded — no \`luminality-release-bot\` App token; falling back to \`GITHUB_TOKEN\`.**"
              echo ""
              echo "The merge will be attributed to \`github-actions[bot]\` and will emit **no \`push\` event**, so push-triggered and \`workflow_run\`-chained workflows (deploy, sentry-release) **will not fire** for it."
              echo ""
              echo "Check, in order:"
              echo "1. the \`luminality-release-bot\` App is installed on this repository;"
              echo "2. \`RELEASE_BOT_CLIENT_ID\` (org variable) and \`RELEASE_BOT_PRIVATE_KEY\` (org secret) are both visible to it."
            } >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Fetch Dependabot metadata
        id: metadata
        if: github.actor == 'dependabot[bot]'
        uses: dependabot/fetch-metadata@25dd0e34f4fe68f24cc83900b1fe3fe149efef98 # v3.1.0
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Approve and enable auto-merge for patch/minor
        if: >-
          github.actor == 'dependabot[bot]' && (
          steps.metadata.outputs.update-type == 'version-update:semver-patch' ||
          steps.metadata.outputs.update-type == 'version-update:semver-minor')
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_URL: ${{ github.event.pull_request.html_url }}
          MERGE_TOKEN: ${{ steps.app_token.outputs.token || secrets.GITHUB_TOKEN }}
          MERGE_TOKEN_MODE: ${{ steps.token.outputs.mode }}
        run: |
          set -euo pipefail
          gh pr review --approve "$PR_URL" || true
          GH_TOKEN="$MERGE_TOKEN" gh pr merge --auto --squash "$PR_URL" || echo "::notice::gh pr merge --auto returned non-zero for $PR_URL (e.g. already merged) — the readback below decides"

          # Read the enable back. `gh pr merge --auto` can report success while
          # queueing nothing — fail loudly instead of reading green. MERGED counts
          # as success: when every requirement is already green GitHub merges
          # outright and leaves no pending autoMergeRequest. Up to 3 reads, 5s
          # apart, so read-after-write lag can't turn a real enable red; a read
          # that still fails (or still shows nothing queued) fails the step —
          # it never reads as queued.
          verdict=not-queued
          for attempt in 1 2 3; do
            verdict=$(gh pr view "$PR_URL" --json autoMergeRequest,state \
              --jq 'if .state == "MERGED" or .autoMergeRequest != null then "ok" else "not-queued" end') || verdict=read-failed
            if [ "$verdict" = "ok" ]; then break; fi
            if [ "$attempt" -lt 3 ]; then sleep 5; fi
          done
          if [ "$verdict" != "ok" ]; then
            echo "::error::auto-merge was NOT queued on $PR_URL ($verdict after 3 reads: autoMergeRequest null and state is not MERGED)"
            exit 1
          fi
          echo "auto-merge queued (or already merged) on $PR_URL — merge token: $MERGE_TOKEN_MODE" >> "$GITHUB_STEP_SUMMARY"

      - name: Enable auto-merge — autonomous release-bot PR
        # Positive actor check (not just "!= dependabot"): only the release-bot
        # App may take this path, so a spoofed ci-fix/* branch from any other
        # actor can never reach this step.
        if: github.actor == 'luminality-release-bot[bot]'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_URL: ${{ github.event.pull_request.html_url }}
          MERGE_TOKEN: ${{ steps.app_token.outputs.token || secrets.GITHUB_TOKEN }}
          MERGE_TOKEN_MODE: ${{ steps.token.outputs.mode }}
        run: |
          set -euo pipefail
          # Engine PRs (issue-worker/*, ci-fix/*, sentry-autofix/*) are already
          # vetted by the engine's own guardrails (path guards, diff-line cap,
          # test gate, signed commit) before opening, and legal-sync/* carries
          # only text copied verbatim from the published legal_documents rows —
          # so just enable squash auto-merge and let the repo's required-checks
          # gate do the rest, the same as Dependabot.
          GH_TOKEN="$MERGE_TOKEN" gh pr merge --auto --squash "$PR_URL" || echo "::notice::gh pr merge --auto returned non-zero for $PR_URL (e.g. already merged) — the readback below decides"

          # Read the enable back. `gh pr merge --auto` can report success while
          # queueing nothing — fail loudly instead of reading green. MERGED counts
          # as success: when every requirement is already green GitHub merges
          # outright and leaves no pending autoMergeRequest. Up to 3 reads, 5s
          # apart, so read-after-write lag can't turn a real enable red; a read
          # that still fails (or still shows nothing queued) fails the step —
          # it never reads as queued.
          verdict=not-queued
          for attempt in 1 2 3; do
            verdict=$(gh pr view "$PR_URL" --json autoMergeRequest,state \
              --jq 'if .state == "MERGED" or .autoMergeRequest != null then "ok" else "not-queued" end') || verdict=read-failed
            if [ "$verdict" = "ok" ]; then break; fi
            if [ "$attempt" -lt 3 ]; then sleep 5; fi
          done
          if [ "$verdict" != "ok" ]; then
            echo "::error::auto-merge was NOT queued on $PR_URL ($verdict after 3 reads: autoMergeRequest null and state is not MERGED)"
            exit 1
          fi
          echo "auto-merge queued (or already merged) on $PR_URL — merge token: $MERGE_TOKEN_MODE" >> "$GITHUB_STEP_SUMMARY"
